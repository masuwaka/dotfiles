#!/usr/bin/env zsh
# =============================================================================
# 組織・チーム共通設定ファイル（テンプレート）
# =============================================================================
# このファイルを .zshrc.custom にリネームして使用してください
# GitLab等のプライベートリポジトリで管理可能です
# =============================================================================

# -----------------------------------------------------------------------------
# プロキシ設定
# -----------------------------------------------------------------------------
# export http_proxy="http://proxy.example.com:8080"
# export https_proxy="http://proxy.example.com:8080"
# export no_proxy="localhost,127.0.0.1,.example.com"

# -----------------------------------------------------------------------------
# 組織共通のパス設定
# -----------------------------------------------------------------------------
# export PATH="/opt/tools/bin:$PATH"
# export LD_LIBRARY_PATH="/opt/tools/lib:$LD_LIBRARY_PATH"

# -----------------------------------------------------------------------------
# 内部サービスの設定
# -----------------------------------------------------------------------------
# export GITLAB_HOST="gitlab.example.com"
# export ARTIFACTORY_URL="https://artifactory.example.com"
# export DOCKER_REGISTRY="registry.example.com"
# export INTERNAL_PKG_REPO="https://packages.example.com"

# -----------------------------------------------------------------------------
# 組織共通のエイリアス
# -----------------------------------------------------------------------------
# alias build-tool='/opt/build-tool/bin/build'
# alias deploy='python /opt/deploy-scripts/deploy.py'
# alias vpn-status='systemctl status openvpn'

# -----------------------------------------------------------------------------
# 組織共通の環境変数
# -----------------------------------------------------------------------------
# export ORGANIZATION="example-corp"
# export DEFAULT_REGION="ap-northeast-1"
# export LOG_LEVEL="info"

# -----------------------------------------------------------------------------
# Git設定（組織用）
# -----------------------------------------------------------------------------
# Git プロトコル設定
# git config --global url."https://gitlab.example.com/".insteadOf "git@gitlab.example.com:"

# 組織用Git設定
# git config --global user.name "Your Name"
# git config --global user.email "your.name@company.com"
# git config --global init.defaultBranch "main"

# SSL証明書設定（内部CA使用時）
# git config --global http.sslVerify false
# git config --global http.sslCAInfo "/path/to/company-ca.crt"

# プロキシ設定（Gitでも必要な場合）
# git config --global http.proxy "http://proxy.company.com:8080"
# git config --global https.proxy "http://proxy.company.com:8080"

# -----------------------------------------------------------------------------
# 開発ツールの共通設定
# -----------------------------------------------------------------------------
# export MAVEN_OPTS="-Xmx2048m"
# export GRADLE_USER_HOME="$HOME/.gradle"
# export NPM_REGISTRY="https://npm.example.com"

# -----------------------------------------------------------------------------
# 関数定義（組織共通）
# -----------------------------------------------------------------------------
# function internal-login() {
#     # 内部システムへのログイン処理
#     echo "Logging in to internal systems..."
#     # 実際のログイン処理
# }

# -----------------------------------------------------------------------------
# チーム固有の設定をさらに読み込む場合
# -----------------------------------------------------------------------------
# if [ -f "$HOME/.zshrc.team" ]; then
#     source "$HOME/.zshrc.team"
# fi