#!/usr/bin/env zsh
# =============================================================================
# マシン固有設定ファイル（テンプレート）
# =============================================================================
# このファイルを ~/.zshrc.local にコピーして、マシン固有の設定を記述してください
# .zshrc.local は .gitignore に含まれるため、リポジトリには含まれません
# =============================================================================

# -----------------------------------------------------------------------------
# マシン固有のプロキシ設定（組織設定と異なる場合のみ）
# -----------------------------------------------------------------------------
# export http_proxy="http://home-proxy.example.com:3128"
# export https_proxy="http://home-proxy.example.com:3128"
# export no_proxy="localhost,127.0.0.1,192.168.1.0/24"

# -----------------------------------------------------------------------------
# 環境固有のパス設定
# -----------------------------------------------------------------------------
# export PATH="/opt/custom/bin:$PATH"
# export LD_LIBRARY_PATH="/opt/custom/lib:$LD_LIBRARY_PATH"

# -----------------------------------------------------------------------------
# 認証情報（例: GitLab, Artifactory, Docker Registry）
# -----------------------------------------------------------------------------
# export GITLAB_TOKEN="your-token-here"
# export ARTIFACTORY_USER="username"
# export ARTIFACTORY_PASSWORD="password"
# export DOCKER_REGISTRY="registry.example.com"

# -----------------------------------------------------------------------------
# 環境固有のエイリアス
# -----------------------------------------------------------------------------
# alias vpn-connect='sudo openvpn /path/to/config.ovpn'
# alias internal-tool='/opt/internal/tool/bin/tool'

# -----------------------------------------------------------------------------
# 環境固有の環境変数
# -----------------------------------------------------------------------------
# export ENVIRONMENT="development"
# export LOG_LEVEL="debug"
# export CUSTOM_CONFIG="/etc/custom/config.yaml"

# -----------------------------------------------------------------------------
# Git設定の上書き（マシン固有）
# -----------------------------------------------------------------------------
# 個人用Git設定（自宅など）
# git config --global user.name "Personal Name"
# git config --global user.email "personal@gmail.com"

# SSH設定（マシン固有のキー使用時）
# git config --global core.sshCommand "ssh -i ~/.ssh/personal_key"

# 特定ディレクトリでの設定切り替え例
# git config --global includeIf.gitdir:~/work/.path ~/.gitconfig-work
# git config --global includeIf.gitdir:~/personal/.path ~/.gitconfig-personal

# -----------------------------------------------------------------------------
# マシン固有の開発ツール設定
# -----------------------------------------------------------------------------
# export JAVA_HOME="/usr/lib/jvm/java-17-openjdk"  # マシン固有のJavaバージョン
# export DOCKER_HOST="unix:///var/run/docker.sock"  # ローカルDocker設定

# -----------------------------------------------------------------------------
# 関数定義（環境固有）
# -----------------------------------------------------------------------------
# function sync-upstream() {
#     # upstreamリポジトリから最新を取得
#     git fetch upstream
#     git checkout main
#     git merge upstream/main
#     git push origin main
# }

# -----------------------------------------------------------------------------
# 条件付き設定（ネットワーク環境による切り替え）
# -----------------------------------------------------------------------------
# if ping -c 1 internal.server.com &> /dev/null; then
#     # 内部ネットワーク環境での設定
#     export INTERNAL_NETWORK=true
#     source ~/.zshrc.internal
# fi